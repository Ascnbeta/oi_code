#include <bits/stdc++.h>
using namespace std;
int T,n,a[100005];
int main () {
    cin >> T;
    while (T--) {
        cin >> n;
        for (int i = 1; i <= n; i++) {
            cin >> a[i];
        }
        int l = 1, r = 1e9;
        while (l <= r) {
            int mid = (l + r) >> 1;
            
        }
    }
    return 0;
}