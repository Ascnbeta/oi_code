#include <iostream>
using namespace std;
int main(){
	freopen("assign.in" , "r" , stdin);
	freopen("assign.out" , "w" , stdout);
	int t;cin >> t;
	for(int ques =1 ; ques <= t ;++ques){
		int n , m , v;
		cin >> n >> m >> v;
		for(int i = 1 ;  <= m ; ++i){
			int u , v;
			cin >> u >> v;
		}
		cout << 0 << endl;
	}
	return 0;
}
