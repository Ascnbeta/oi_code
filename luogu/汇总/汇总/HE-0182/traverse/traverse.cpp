#include <iostream>

int c, T, n, k, o = 0;

int main()
{
	using namespace std;
	
	freopen("traverse.in", "r", stdin);
	freopen("traverse.out", "w", stdout);
	
	cin >> c >> T;
	
	while (T--)
	{
		o = 0;
		
		cin >> n >> k;
		
		o = 2;
		
		cout << o << endl;
	}
	
	fclose(stdin);
	fclose(stdout);
	
	return 0;
}
