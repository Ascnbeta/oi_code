#include <bits/stdc++.h>
#define int long long
using namespace std;
int q,x;//1721424
bool check(int x) {
	
}
signed main () {
	ios::sync_with_stdio(0);
	cin.tie(0),cout.tie(0);
	cin >> q;
	while (q--) {
		cin >> x;
		if (x <= 1721423) {
			int l = 1,r = 4713;
			while (l <= r) {
				int mid = (l + r) >> 1;
				if (check(mid))
			}
		}
	}
	return 0;
}
